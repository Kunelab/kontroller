# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# android.useAndroidX was dropped: the app has no dependencies at all and deliberately
# avoids AndroidX (see app/build.gradle), so the flag governed nothing.
# android.nonTransitiveRClass is the default from AGP 8 and was likewise redundant.

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
